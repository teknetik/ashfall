# Frozen concept frames

Phase 2 has frozen the eight numbered PNGs. Keep accepted frames immutable;
write revisions under new names. `manifest.json` records exact hashes and sizes.
The three prompt JSON files preserve generation inputs and provenance.

These are visual targets; generated background placements do not replace the
runtime layout. See `docs/phase-2.md` for acceptance and comparison constraints.
Phase 0 probe screenshots remain pipeline evidence, not city visual acceptance.
